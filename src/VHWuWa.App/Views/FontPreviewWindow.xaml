<Window x:Class="VHWuWa.App.Views.FontPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Xem trước font" Width="1120" Height="680" MinWidth="760" MinHeight="480"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource ApplicationBackgroundBrush}">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Xem trước kích thước lớn" FontSize="20" FontWeight="Bold" />
                <TextBlock Text="{Binding PreviewMessage}" Opacity="0.68" Margin="0,2,0,0" />
            </StackPanel>
            <Button Grid.Column="1" Content="Đóng" Click="Close_Click" Padding="14,6" VerticalAlignment="Center" />
        </Grid>

        <Grid Grid.Row="1" Margin="0,14,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="300" />
            </Grid.ColumnDefinitions>
            <TextBox Text="{Binding SampleText, UpdateSourceTrigger=PropertyChanged}" Padding="10,7"
                     Background="{DynamicResource ControlFillColorDefaultBrush}"
                     Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                     BorderBrush="{DynamicResource ControlElevationBorderBrush}" />
            <Grid Grid.Column="1" Margin="12,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Content="−" Command="{Binding DecreasePreviewSizeCommand}" Width="34" />
                <Slider Grid.Column="1" Minimum="12" Maximum="72" TickFrequency="2" IsSnapToTickEnabled="True"
                        Value="{Binding PreviewFontSize, Mode=TwoWay}" Margin="9,0" VerticalAlignment="Center" />
                <Button Grid.Column="2" Content="+" Command="{Binding IncreasePreviewSizeCommand}" Width="34" />
                <Button Grid.Column="3" Content="{Binding PreviewFontSizeLabel}" Command="{Binding ResetPreviewSizeCommand}"
                        MinWidth="58" Margin="8,0,0,0" />
            </Grid>
        </Grid>

        <Border Grid.Row="2" Background="#282A36" BorderBrush="#3F4454" BorderThickness="1"
                CornerRadius="10" Padding="14">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Image Source="{Binding PreviewImage}" Stretch="None" HorizontalAlignment="Left" VerticalAlignment="Top"
                       RenderOptions.BitmapScalingMode="HighQuality" SnapsToDevicePixels="True" />
            </ScrollViewer>
        </Border>

        <TextBlock Grid.Row="3" Text="Dùng thanh trượt hoặc nút − / + để kiểm tra font ở nhiều cỡ chữ."
                   Opacity="0.6" FontSize="11" Margin="2,8,0,0" />
    </Grid>
</Window>
