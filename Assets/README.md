# Assets

Thư mục chứa tài nguyên giao diện (logo, icon).

- `app.ico`, `logo.png`: thêm file thật của bạn vào đây rồi bật lại `<ApplicationIcon>` trong `VHWuWa.App.csproj`.
- Hiện để trống (placeholder) để repository không kèm tài sản bản quyền.
